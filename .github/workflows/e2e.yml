name: End-to-End Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Cypress run (build + serve + tests)
        uses: cypress-io/github-action@v6
        with:
          build: npm run build # build once
          start: npm run start:ci # static + relay
          wait-on: http://localhost:9000/p2pEditor/
          wait-on-timeout: 180 # give the static server 3 min max
          browser: chrome
          headless: true
        env:
          CYPRESS_baseUrl: http://localhost:9000/p2pEditor/
          CYPRESS_defaultCommandTimeout: 10000
